apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "helm-charts.fullname" . }}
  labels:
  {{- include "helm-charts.labels" . | nindent 4 }}
data:
  appInitContainerRepo: "{{ .Values.image.repo | quote }}"
  appInitContainerTag: "{{ .Values.image.tag | quote }}"