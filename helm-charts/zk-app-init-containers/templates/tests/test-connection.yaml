apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zk-app-init-containers.fullname" . }}-test-connection"
  labels:
    {{- include "zk-app-init-containers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zk-app-init-containers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
